{
    "configurations": [
        {
            "name": "amp (package-aware)",
            "type": "debugpy",
            "request": "launch",

            "module": "waveform_analysis.factors.amp",

            "cwd": "${workspaceFolder}",
            "env": { "PYTHONPATH": "${workspaceFolder}" }
        }
    ]
}